/*jshint expr:true*/
var _ = require('lodash');
var should = require('should');
var agent = require('src/util/test/agent');

before(function (done) {
  //mongoose.connect(config.db.mongodb);
  done();
});

describe('Daemon API', function () {
  it('is not yet tested', function (done) {
    done();
  });
});
